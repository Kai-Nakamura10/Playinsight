<div class="-z-10 relative w-full h-56 bg-center bg-contain sm:h-72 md:min-h-[calc(100vh-134px)] md:bg-cover" style="background-image: url('<%= asset_path('Frame 6.png') %>');">
</div>
<body class="min-h-screen bg-slate-50">
  <%= yield %>

  <div class="fixed bottom-4 right-4 z-50 flex flex-col items-end space-y-2">
    <div id="help-panel" class="hidden origin-bottom-right transform transition-all duration-200 w-64 rounded-2xl bg-white shadow-2xl border border-slate-100 p-4 text-sm text-slate-700 scale-95 opacity-0">
      <p class="text-xs font-semibold text-slate-500 uppercase tracking-wide">ヘルプセンター</p>
      <p class="mt-2 leading-relaxed text-slate-600">
        使い方ガイドで基本操作を確認し、困ったときはサポートにご連絡ください。
      </p>
      <div class="mt-4 space-y-3">
        <%= link_to "使い方ガイドへ移動", how_to_path, class: "flex items-center justify-between px-3 py-2 rounded-xl bg-indigo-50 text-indigo-700 text-sm font-semibold hover:bg-indigo-100" %>
        <a href="mailto:support@example.com" class="flex items-center justify-between px-3 py-2 rounded-xl bg-slate-100 text-slate-700 text-sm font-semibold hover:bg-slate-200">
          サポートに連絡
          <span class="text-xs text-slate-500">support@example.com</span>
        </a>
      </div>
    </div>

    <button id="help-toggle" type="button" class="relative flex items-center gap-3 px-4 py-2 rounded-full bg-gradient-to-r from-indigo-600 to-blue-500 shadow-lg text-white text-sm font-semibold hover:scale-105 focus:ring-4 focus:ring-indigo-300 transition">
      <span class="flex items-center justify-center w-7 h-7 rounded-full bg-white/20 border border-white/30 text-base font-bold">?</span>
      <span>ヘルプ</span>
      <span class="absolute -top-1 -right-1 flex h-4 w-4 items-center justify-center rounded-full bg-rose-500 text-[10px] font-bold shadow">!</span>
    </button>
  </div>

  <script>
    (function () {
      let initialized = false;

      function setupHelpButton() {
        if (initialized) return;
        const toggle = document.getElementById('help-toggle');
        const panel = document.getElementById('help-panel');
        if (!toggle || !panel) return;

        toggle.addEventListener('click', function () {
          const isHidden = panel.classList.toggle('hidden');
          panel.classList.toggle('scale-100', !isHidden);
          panel.classList.toggle('opacity-100', !isHidden);
          panel.classList.toggle('scale-95', isHidden);
          panel.classList.toggle('opacity-0', isHidden);
        });

        initialized = true;
      }

      document.addEventListener('DOMContentLoaded', setupHelpButton);
      document.addEventListener('turbo:load', setupHelpButton);
      document.addEventListener('turbolinks:load', setupHelpButton);
      document.addEventListener('turbo:before-cache', () => {
        initialized = false;
      });
      document.addEventListener('turbolinks:before-cache', () => {
        initialized = false;
      });
    })();
  </script>
</body>
